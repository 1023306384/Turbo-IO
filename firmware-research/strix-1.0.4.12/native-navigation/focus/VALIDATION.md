# FOCUS-04 发布复核 · 2026-09-25

## 已执行

- 从本目录公开源码，以固定 OHOS clang 重建：AP 9,558,216 字节、SHA-256 `40b08299d66b08a909c9167400f2f660b17904d6b20026477ac470e860942a24`，与用户已实刷候选一致。
- 对发布原样 ZIP 检查 15 个成员；其余 13 个负载保持原厂哈希，清单 AP Size/MD5 正确，烧写元数据不变。
- 负向测试拒绝：改 AP、改非 AP、改清单、额外路径；精确 ZIP 与公开重建 ZIP 分开判定。
- ARM 模拟运行真实链接代码，验证四项菜单、12 个入口/120 场景、分配失败、重复销毁、绘制忙延迟释放、父节点删除、失效回调注册清理；并运行番茄、阅读、音乐、导航、显示与诊断回归。
- 番茄状态与布局主机测试通过 AddressSanitizer / UndefinedBehaviorSanitizer；含暂停、息屏、时钟回绕、重复/过期命令、完成一次、边界与不重叠检查。
- 公开 iOS 插件在本机 arm64 编译成功（仍有 SDK 弃用等警告）。打包的正反向校验、宿主/高德资源和源码审计测试共 33 项通过；TFP1 校验测试实际读入发布 ZIP，篡改一字节拒绝。
- 源码路径/服务租户/凭据扫描无发现；Gitleaks 扫描无泄漏发现。所有图片均作为设计素材或无私人配置界面检查；不发布日志、私用配置、Cookie、签名、IPA。扫描不是绝对隐私保证。

## 实机与未验收项

用户已将精确 FOCUS-04 候选刷入眼镜，并反馈“完美”。该反馈不是长时耗电、多机型、深度休眠、升级中断或救砖测试。

公开手机版移除私有凭据/网页正文组件，复用可编辑中性身份与公开 TTS 配置；编译与静态/打包检查完成，未重新安装脱敏后的整包做镜片端全量验收。海报不能替代这些验收。

**无本次额外真机操作或刷写。** 固件 Release 标为 Pre-release；禁止用测试通过推导“永不变砖”。
